.slider
  - data.graduates.each do |grad|
    .slider-content
      %figure.with-caption
        .image--centered
          = image_tag "graduates/#{grad[:image]}"
        %figcaption
          %p= grad[:name]
          %p= grad[:summary]
          %p= grad[:cohort]
