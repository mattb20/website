.lazy-slider
  - data.graduates.each do |grad|
    .slider-content
      %figure.with-caption
        .image--centered
          %img{'data-lazy' => image_path("graduates/#{grad[:image]}")}
        %figcaption
          %p= grad[:name]
          %p= grad[:summary]
          %p= grad[:cohort]
